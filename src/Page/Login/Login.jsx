import React from 'react'

export default function Login() {
    // lấy value formit => post lên API(QLUserAction) => tạo action dispatch lên reducer lưu localstorage duy trì đăng nhập (QLUserReducer)=> render UI login (Header)
    return (
        <div>Login</div>
    )
}
